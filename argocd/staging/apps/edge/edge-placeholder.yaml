apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-staging-edge-placeholder
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: banking-platform
    app.kubernetes.io/managed-by: argocd
  annotations:
    platform.banking.io/environment: staging
    platform.banking.io/layer: edge
# GIT-01 skeleton only.
# Replace with concrete edge service Applications in GIT-07.
