Controllers and operators for staging

This directory is intended for Argo CD Application manifests that deploy Kubernetes controllers or operators used by the staging environment.

If you add controller apps here, include a README entry describing install order and any cluster-scoped prerequisites.
