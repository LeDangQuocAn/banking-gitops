apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-prod-core-discovery-client
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    platform.banking.io/layer: core
spec:
  project: banking-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: banking-prod
  source:
    repoURL: https://github.com/LeDangQuocAn/banking-gitops.git
    targetRevision: production
    path: charts/banking-spring-boot
    helm:
      releaseName: discovery-client-prod
      valueFiles:
        - ../../environments/prod/common-values.yaml
        - ../../environments/prod/discovery-client-values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
