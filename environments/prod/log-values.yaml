image:
  repository: ghcr.io/your-org/log-service
  tag: "1.0.0"

replicaCount: 2

container:
  port: 8089

service:
  port: 8089
  targetPort: http

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: log.banking.example.com
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
