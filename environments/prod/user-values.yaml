image:
  repository: ghcr.io/your-org/user-service
  tag: "1.0.0"

replicaCount: 2

container:
  port: 8082

service:
  port: 8082
  targetPort: http

ingress:
  enabled: false
  className: "alb"
  annotations: {}
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
