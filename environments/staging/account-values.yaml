image:
  repository: ghcr.io/your-org/account-service
  tag: "staging-latest"

replicaCount: 1

container:
  port: 8084

service:
  port: 8084
  targetPort: http

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: account.staging.banking.local
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
