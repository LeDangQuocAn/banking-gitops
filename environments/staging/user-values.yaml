image:
  repository: ghcr.io/your-org/user-service
  tag: "staging-latest"

replicaCount: 1

container:
  port: 8082

service:
  port: 8082
  targetPort: http

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: user.staging.banking.local
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
